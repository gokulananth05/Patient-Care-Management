export const adminLogin = async (username, password) => {
  // Simulate a backend call
  return username === 'admin' && password === 'admin';
};

export const createDoctor = async (doctorDetails) => {
  console.log('Doctor created:', doctorDetails);
};

